@model TrivialPursuit.Models.Question.QuestionDetail

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

@Html.Partial("_QuestionDetailPartial", Model)

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index") |
    @Html.ActionLink("Add Answer", "AddAnswer", new { id = Model.Id })
</p>