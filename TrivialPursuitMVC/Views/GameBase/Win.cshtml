@model TrivialPursuit.Models.GameBaseModels.GameEditModel

@{
    ViewBag.Title = "Win";
}

<h2>@TempData["Winner"]</h2>
@if (User.Identity.IsAuthenticated)
{
    <p><a href='@Url.Action("Index", "GameBase")' class="btn btn-primary">Play Again</a></p>
}

